{% extends 'base.html.twig' %}

{% block bread %}
    → Login
{% endblock %}

{% block body %}
<form action="{{ path('app_login') }}" method="post">
    <div>
        <label class="block text-sm text-gray-700 dark:text-gray-300">Email</label>
        <input type="text"
               name="_username"
               class="block w-full shadow-sm border-gray-300 dark:border-transparent dark:text-gray-800 rounded-md border p-2 mt-1 mb-2" value="{{ lastUsername }}"/>
    </div>
    <div>
        <label class="block text-sm text-gray-700 dark:text-gray-300">Password</label>
        <input type="password"
               name="_password"
               class="block w-full shadow-sm border-gray-300 dark:border-transparent dark:text-gray-800 rounded-md border p-2 mt-1 mb-2" />
    </div>

    <input type="hidden"
           name="_target_path" value="/micro-post"/>

    <div>
        <button type="submit"
                class="block w-full shadow-sn borer-transparent p-2 mt-2 mb-1 rounded-md border bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-400">Login</button>
    </div>

</form>
{% endblock %}